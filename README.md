# Playbooks that will create a ldapsync container with cronjobs that runs every 5 minutes for syncing ldap group to OCP. 

Test first with your AD / Ldap setup before using.

running:

ansible-playbook main.yaml
